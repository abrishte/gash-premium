# Gash Truck Repair Website

Premium static website built with HTML, CSS, and vanilla JavaScript.

## Open locally
Open `index.html` in your browser, or use the VS Code Live Server extension.

## GitHub Pages
Upload all files to the repository root, then enable Pages from the `main` branch.
The included `CNAME` file points to `gashtrucksrepair.com`.
